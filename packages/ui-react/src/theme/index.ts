"use client";

// Framework-agnostic theme foundation (OKLCH utils, tokens, defaults, store).
export * from "@wystack/ui-core/theme";

// React provider and hook.
export { StduiProvider, useTheme } from "./provider";
